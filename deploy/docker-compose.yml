<<<<<<< HEAD
# deploy/docker-compose.yml
# Frontend removed - Backend only

services:
  postgres:
    image: postgis/postgis:15-3.4
    container_name: logisecure-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-logisecure}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secure_password}
      POSTGRES_DB: ${POSTGRES_DB:-logisecure}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - logisecure-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-logisecure}"]
      interval: 30s
      timeout: 10s
      retries: 5

=======
# ==============================================================================
# LOGISECURE AI - LOCAL / ON-PREMISE ORCHESTRATION
#
# One command from the repository root boots the whole stack:
#   docker compose -f deploy/docker-compose.yml up --build
#
# Backend will be live at  http://localhost:8000  (interactive docs at /docs)
# ==============================================================================

name: logisecure

services:
>>>>>>> 0436cee945067eb9dda6c9d62f2903ba4a7cb103
  backend:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.backend
<<<<<<< HEAD
    container_name: logisecure-backend
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-logisecure}:${POSTGRES_PASSWORD:-secure_password}@postgres:5432/${POSTGRES_DB:-logisecure}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-llama-3.1-8b-instant}
      - LLM_TEMPERATURE=${LLM_TEMPERATURE:-0.3}
      - DEBUG=${DEBUG:-False}
      - USE_MOCK_DATA=${USE_MOCK_DATA:-True}
      - ENABLE_AMD_ROCM=${ENABLE_AMD_ROCM:-False}
      - HOST=0.0.0.0
      - PORT=8000
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - logisecure-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  logisecure-network:
    driver: bridge

volumes:
  postgres_data:
=======
    image: logisecure-backend:latest
    container_name: logisecure-backend
    ports:
      - "${PORT:-8000}:8000"
    # API keys & flags come from backend/.env (created by scripts/setup.*).
    # The file is optional so the stack still boots with safe defaults / mocks.
    env_file:
      - path: ../backend/.env
        required: false
    environment:
      BACKEND_ENV: ${BACKEND_ENV:-development}
      USE_AMD_ROCM: "False"   # Hackathon mode: inference goes through cloud APIs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  # ── Frontend (React/Vite) ────────────────────────────────────────────────
  # The frontend/ directory does not exist yet. When it lands, uncomment:
  #
  # frontend:
  #   build:
  #     context: ..
  #     dockerfile: deploy/Dockerfile.frontend
  #   container_name: logisecure-frontend
  #   ports:
  #     - "5173:80"
  #   depends_on:
  #     backend:
  #       condition: service_healthy

  # ── Production target only (AMD ROCm local inference) ───────────────────
  # Requires AMD kernel drivers on the host; see README before enabling.
  #
  # logisecure-agent:
  #   image: rocm/rocm-terminal:latest
  #   network_mode: "host"
  #   devices:
  #     - "/dev/kfd:/dev/kfd"
  #     - "/dev/dri:/dev/dri"
  #   security_opt:
  #     - "seccomp=unconfined"
  #   volumes:
  #     - ../:/workspace
>>>>>>> 0436cee945067eb9dda6c9d62f2903ba4a7cb103
