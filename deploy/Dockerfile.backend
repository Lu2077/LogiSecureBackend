# Build only the backend
docker build -f Dockerfile.backend -t logisecure-backend ..

# Run the backend container
docker run -d \
  --name logisecure-backend \
  -p 8000:8000 \
  -e GROQ_API_KEY=${GROQ_API_KEY} \
  logisecure-backend